"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveFuncTypeUnpack = void 0;
const resolveDescriptors_1 = require("../../types/resolveDescriptors");
function resolveFuncTypeUnpack(descriptor, name, ctx, optional = false) {
    // String
    if (typeof descriptor === 'string') {
        return resolveFuncTypeUnpack((0, resolveDescriptors_1.getType)(ctx.ctx, descriptor), name, ctx);
    }
    // TypeRef
    if (descriptor.kind === 'ref') {
        return resolveFuncTypeUnpack((0, resolveDescriptors_1.getType)(ctx.ctx, descriptor.name), name, ctx, descriptor.optional);
    }
    if (descriptor.kind === 'map') {
        return name;
    }
    if (descriptor.kind === 'void') {
        throw Error('Void type is not allowed in function arguments: ' + name);
    }
    // TypeDescription
    if (descriptor.kind === 'primitive') {
        return name;
    }
    else if (descriptor.kind === 'struct') {
        if (optional || descriptor.fields.length === 0) {
            return name;
        }
        else {
            return '(' + descriptor.fields.map((v) => resolveFuncTypeUnpack(v.type, name + `'` + v.name, ctx)).join(', ') + ')';
        }
    }
    else if (descriptor.kind === 'contract') {
        if (optional || descriptor.fields.length === 0) {
            return name;
        }
        else {
            return '(' + descriptor.fields.map((v) => resolveFuncTypeUnpack(v.type, name + `'` + v.name, ctx)).join(', ') + ')';
        }
    }
    // Unreachable
    throw Error('Unknown type: ' + descriptor.kind);
}
exports.resolveFuncTypeUnpack = resolveFuncTypeUnpack;
