"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.cast = void 0;
const resolveDescriptors_1 = require("../../types/resolveDescriptors");
function cast(from, to, expression, ctx) {
    if (from.kind === 'ref' && to.kind === 'ref') {
        if (from.name !== to.name) {
            throw Error('Impossible');
        }
        if (!from.optional && to.optional) {
            let type = (0, resolveDescriptors_1.getType)(ctx.ctx, from.name);
            if (type.kind === 'struct') {
                ctx.used(`__gen_${type.name}_as_optional`);
                return `__gen_${type.name}_as_optional(${expression})`;
            }
        }
    }
    return expression;
}
exports.cast = cast;
